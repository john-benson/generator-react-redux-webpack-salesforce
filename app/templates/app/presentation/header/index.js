require('./styles.scss');

import React from 'react';
import { Link } from 'react-router';

const Header = () => {
  return (
    <div className="header">
      <h2>Your Cases</h2>
    </div>
  );
};

export default Header;
